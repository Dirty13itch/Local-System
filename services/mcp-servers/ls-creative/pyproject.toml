[project]
name = "ls-creative"
version = "0.1.0"
description = "Local-System creative MCP server — ComfyUI image generation for any coding tool"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0",
    "httpx>=0.27",
]

[project.scripts]
ls-creative = "ls_creative.server:main"
