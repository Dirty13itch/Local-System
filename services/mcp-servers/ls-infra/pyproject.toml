[project]
name = "ls-infra"
version = "0.1.0"
description = "Local-System infrastructure MCP server — cluster monitoring for any coding tool"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0",
    "httpx>=0.27",
    "pydantic>=2.0",
]

[project.scripts]
ls-infra = "ls_infra.server:main"
